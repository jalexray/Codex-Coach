{
  "name": "codex-coach",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first Codex coaching plugin CLI.",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "codex-coach": "./bin/codex-coach",
    "codex-coach-mcp": "./bin/codex-coach-mcp"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run typecheck && npm run build --silent && node --test dist/*/*.test.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "sql.js": "^1.14.1"
  },
  "devDependencies": {
    "@types/node": "^20.17.10",
    "@types/sql.js": "^1.4.11",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
